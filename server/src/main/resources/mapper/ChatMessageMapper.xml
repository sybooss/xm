<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.user.returnsassistant.mapper.ChatMessageMapper">
    <select id="listBySessionId" resultType="com.user.returnsassistant.pojo.ChatMessage">
        select * from chat_message where session_id=#{sessionId} order by seq_no asc
    </select>

    <select id="listRecentBySessionId" resultType="com.user.returnsassistant.pojo.ChatMessage">
        select * from (
            select * from chat_message
            where session_id=#{sessionId}
            order by seq_no desc
            limit #{limit}
        ) recent
        order by seq_no asc
    </select>

    <select id="maxSeqNo" resultType="int">
        select coalesce(max(seq_no), 0) from chat_message where session_id=#{sessionId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into chat_message(session_id, role, content, message_type, seq_no, reply_to_id, intent_code, source_type)
        values(#{sessionId}, #{role}, #{content}, coalesce(#{messageType}, 'TEXT'), #{seqNo}, #{replyToId}, #{intentCode}, #{sourceType})
    </insert>
</mapper>
