<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.user.returnsassistant.mapper.AfterSaleDiagnosisMapper">
    <sql id="BaseSelect">
        select d.*,
               o.order_no,
               o.product_name,
               o.sku_name,
               u.display_name as user_display_name
        from after_sale_diagnosis d
        left join demo_order o on d.order_id=o.id
        left join user_account u on d.user_id=u.id
    </sql>

    <select id="getById" resultType="com.user.returnsassistant.pojo.AfterSaleDiagnosis">
        <include refid="BaseSelect"/>
        where d.id=#{id}
    </select>

    <select id="getLatestBySessionId" resultType="com.user.returnsassistant.pojo.AfterSaleDiagnosis">
        <include refid="BaseSelect"/>
        where d.session_id=#{sessionId}
        order by d.created_at desc, d.id desc
        limit 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into after_sale_diagnosis(
            diagnosis_no, application_id, session_id, order_id, user_id, issue_text,
            suggested_service_type, decision_level, reason_summary, required_evidence,
            solution_options_json, ai_summary, ai_status, ai_error_message
        )
        values(
            #{diagnosisNo}, #{applicationId}, #{sessionId}, #{orderId}, #{userId}, #{issueText},
            #{suggestedServiceType}, #{decisionLevel}, #{reasonSummary}, #{requiredEvidence},
            #{solutionOptionsJson}, #{aiSummary}, #{aiStatus}, #{aiErrorMessage}
        )
    </insert>

    <update id="bindApplication">
        update after_sale_diagnosis
        set application_id=#{applicationId}
        where id=#{id}
    </update>
</mapper>
