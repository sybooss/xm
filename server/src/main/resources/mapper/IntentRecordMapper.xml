<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.user.returnsassistant.mapper.IntentRecordMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into intent_record(session_id, message_id, intent_code, intent_name, confidence, method, slots_json)
        values(#{sessionId}, #{messageId}, #{intentCode}, #{intentName}, #{confidence}, #{method}, #{slotsJson})
    </insert>

    <select id="listBySessionId" resultType="com.user.returnsassistant.pojo.IntentRecord">
        select * from intent_record where session_id=#{sessionId} order by created_at desc
    </select>
</mapper>
